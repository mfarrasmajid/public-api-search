# Base URL of the search API as seen from the browser.
# Leave empty when running behind nginx (same origin, /api is proxied).
VITE_API_BASE_URL=

# Port the browser talks to, used by Vite HMR when running behind nginx.
VITE_HMR_CLIENT_PORT=8080
